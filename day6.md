# JSON Web Token (JWT)
* JWT starts with "ey" , divided into 3 sections(header part/alogorithm , payload section, )
* we can generate tokens through JWT
* JWT is used for gnerationg tokens not only in NodeJS but also in .net and others

* token ---> identity of the user (just like citizenship or symbol no in exam)
* cookie ---> stores token
* token are always in encrypted format


## Refactoring

* converting complex code into modules